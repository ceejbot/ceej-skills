interface:
  display_name: "Set up project memory"
  short_description: "Bootstrap trivia memory for a project"
dependencies:
  tools:
    - type: "mcp"
      value: "trivia"
      description: "Trivia semantic memory MCP server"
