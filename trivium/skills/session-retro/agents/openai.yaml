interface:
  display_name: "Run a session retro"
  short_description: "Capture durable lessons from a session"
dependencies:
  tools:
    - type: "mcp"
      value: "trivia"
      description: "Trivia semantic memory MCP server"
