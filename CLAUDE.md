# ceej-skills

## Project memory

This project uses the trivia MCP. All memories are tagged `project:ceej-skills`. Open working sessions with the `session-start` skill, which recalls the current focus and top lessons under that tag. Add new lessons via the `session-retro` skill.

Use lowercased tags.
