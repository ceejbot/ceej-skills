---
description: Draw an Oblique Strategy from Eno & Schmidt's deck
allowed-tools: Bash(oblique)
---

Strategy drawn: !`oblique`

Present the strategy above to the user verbatim, on its own line, with no commentary. If the command failed because `oblique` is not installed, instead tell the user to install it with `brew install ceejbot/tap/oblique` or `cargo install --git https://github.com/ceejbot/oblique`.
