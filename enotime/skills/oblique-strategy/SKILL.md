---
name: oblique-strategy
description: Draw one of Brian Eno and Peter Schmidt's Oblique Strategies when the user asks for one, begins a creative session, feels stuck, or wants a lateral prompt.
---

# ENOTIME

Run `oblique` once and present its output verbatim on its own line. Add no
commentary unless the user asks to discuss or apply the strategy.

If `oblique` is unavailable, say that the skill needs the CLI and give both
installation choices:

```text
brew install ceejbot/tap/oblique
cargo install oblique
```

Do not install it unless the user asks.
